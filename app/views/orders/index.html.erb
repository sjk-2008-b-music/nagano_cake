<div class="container">
     <div class="row">
		<div class="body">

			<h1>注文履歴一覧</h1>

				<table class="table">
	  				<thead>
	    				<tr>
	    					<th>注文日</th>
					    	<th>配送先</th>
						    <th>注文商品</th>
						    <th>支払金額</th>
						    <th>ステータス</th>
						    <th>注文詳細</th>
						    <th colspan="3"></th>
					    </tr>
					</thead>
					<tbody>
					<% @orders.each do |order| %>
						<tr>
							<td><%= order.created_at.strftime("%Y-%m-%d") %></td>
							<td><%= order.address %></td>


							<td>
								<% order.order_details.each do |order_detail| %>
									<%= order_detail.item.name %>
								<% end %>
							</td>

							<td><%= order.total_payment %></td>
							<td><%= order.status %></td>
							<td><%= link_to "表示する", order_path(order.id) %></td>
					<% end %>
						<tr>
	  				</tbody>
				</table>
		</div>
	</div>
</div>
<!--
<%# @orders.each do |order| %>
<%#= order.name %>
<%#= order.order_details.each do |f| %>
<%#= f.making_status %>

<%# end %>
<%# end %>

注文先に<%# @orders.each do |order| %>
注文詳細
 -->



<!--
<table class="table table-hover table-inverse">
					<thead>
						<tr>
							<th>商品ID</th>
							<th>商品名</th>
							<th>税抜価格</th>
							<th>ジャンル</th>
							<th>ステータス</th>
							<th colspan="3"></th>
						</tr>
					</thead>
				<tbody>

					<%# @items.each do |item| %>
						<tr>
							<td><%#= item.id %></td>
							<td><%#= link_to item.name, admins_item_path(item.id), class: "item_#{item.id}" %></td>
							<td><%#= item.price %></td>
							<td><%#= item.genre.name %></td>
								<%# if item.is_active %>
							<td>販売中</td>
								<%# else %>
							<td>販売停止中</td>
								<%# end %>
						</tr>
					<%# end %>
				</tbody> -->